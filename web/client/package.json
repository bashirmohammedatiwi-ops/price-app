{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "flutter-web:build": "cd ../../mobile_app && flutter build web --base-href /price/flutter/ && rm -rf ../web/client/public/flutter && cp -R build/web ../web/client/public/flutter"
  },
  "dependencies": {
    "@zxing/browser": "^0.1.5",
    "@zxing/library": "^0.21.3"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  }
}
