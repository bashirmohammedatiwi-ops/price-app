server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location = /price-admin {
    return 301 /price-admin/;
  }

  location /price-admin/ {
    rewrite ^/price-admin/(.*)$ /$1 break;
    try_files $uri $uri/ /index.html;
  }
}
